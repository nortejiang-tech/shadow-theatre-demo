# 开发提示词 · 《影子剧场》日食月食互动科普

> **使用说明**
> 把下面 `═══ 提示词开始 ═══` 到 `═══ 提示词结束 ═══` 之间的全部内容，连同 `01-设计与需求文档.md` 的全文，一起发给开发模型。
> 如果模型的上下文装不下两份文件，**只发这一份也能做对** —— 关键规格已全部内联。
> 如果要分阶段开发，用文末的《分批指令》。

---

═══ 提示词开始 ═══

# 任务

用 **Three.js** 写一个**单文件 HTML** 的三维互动科普小游戏，向一个 **7–9 岁的中国孩子（名字叫"小朋友"）** 讲清楚日食和月食的原理，包括日全食、日偏食、日环食三种日食，以及月全食、月偏食、半影月食三种月食。

运行环境：现代桌面或笔记本电脑上的 Chrome 或 Safari，双击 `.html` 文件直接打开。性能预算非常宽松。

---

# 一、硬性约束（违反任何一条即为不合格）

1. **交付物是一个 `.html` 文件。** 除 three.js 从 CDN 加载外，不允许任何外部资源：不用图片、不用模型文件、不用字体文件、不用音频文件。地球的大陆、月球的环形山、太阳的表面、星空、所有音效，**全部程序化生成**。

2. **代码必须完整可运行。** 不允许出现 `// TODO`、`// 这里可以扩展`、`// 省略实现`、`/* 类似地处理其他情况 */` 之类的占位。每一个被调用的函数都必须有真实实现。每一段文案都必须写全。

3. **不用构建工具、不用框架。** 原生 `<script type="module">` + importmap。UI 用原生 DOM + CSS 浮在 canvas 上。

4. **所有天文常数集中在文件顶部，命名清晰。** 代码里不允许出现来历不明的魔数。

5. **全中文界面。** 文案面向 7–9 岁孩子，短句、口语、第二人称"你"。

6. **控制台无 error、无 warning。**

---

# 二、必须算对的科学规格（这一节是本任务的核心，请逐条核对）

## 2.1 天文常数（照抄）

```js
const AU      = 149597870;   // km
const R_SUN   = 696000;      // km
const R_EARTH = 6371;        // km
const R_MOON  = 1737.4;      // km

const D_SUN_MEAN = 149597870, D_SUN_PERI = 147100000, D_SUN_APO = 152100000;
const D_MOON_MEAN = 384400,   D_MOON_PERI = 356500,   D_MOON_APO = 406700;

const MOON_INCL     = 5.145;      // 度，白道对黄道倾角
const SYNODIC_MONTH = 29.530589;  // 天
```

## 2.2 视直径

```js
function angularDiameter(R, d) { return 2 * Math.asin(R / d); }  // 弧度
```

**必须与下表一致（误差 < 0.5%），实现后请自测：**

| 对象 | 距离 (km) | 角直径 |
|---|---|---|
| 太阳（平均） | 149,597,870 | **0.5331°** |
| 太阳（近日点） | 147,100,000 | **0.5422°** |
| 太阳（远日点） | 152,100,000 | **0.5244°** |
| 月球（平均） | 384,400 | **0.5179°** |
| 月球（近地点） | 356,500 | **0.5585°** |
| 月球（远地点） | 406,700 | **0.4895°** |

关键结论：月球角直径 0.4895°–0.5585°，太阳角直径 0.5244°–0.5422°，**两者重叠**。月亮看起来比太阳大 → 可能全食；比太阳小 → 只能环食。

⚠️ 注意平均值：月球 0.5179° **小于** 太阳 0.5331°。**平均而言月亮盖不满太阳** —— 所以日环食其实比日全食略常见。这与 2.3 节"平均情况下影锥够不到地球"是同一件事的两种说法。

## 2.3 影锥（本作的两根支柱）

```js
// dist = 光源到「遮挡天体」的距离 —— 见下方警告
function umbraLength(rOccluder, rSource, dist) {
  return rOccluder * dist / (rSource - rOccluder);
}
function umbraHalfAngle(rOccluder, rSource, dist) {
  return Math.atan((rSource - rOccluder) / dist);
}
function penumbraHalfAngle(rOccluder, rSource, dist) {
  return Math.atan((rSource + rOccluder) / dist);
}
```

⚠️⚠️ **`dist` 是太阳到「遮挡天体」的距离，不是日地距离。极易搞错：**
- 算**月球**影锥（日食）：`dist` = 日月距离 = 149,597,870 − 384,400 = **149,213,470 km**
- 算**地球**影锥（月食）：`dist` = 日地距离 = **149,597,870 km**

**必须算出这两个数：**

- **月球本影锥长 ≈ 373,408 km**
- **地球本影锥长 ≈ 1,382,030 km**

**自洽性检查（务必实现并在 `runSelfTest()` 里跑）**：月球本影锥长 `L` 应当**精确等于**"月球视直径恰好等于太阳视直径时的地月距离"。这不是巧合，是同一个几何关系的两种表述：

```
R_moon/d = R_sun/(D_日月 + d)  ⟹  d = R_moon × D_日月/(R_sun − R_moon) = L
```

用二分法解出角径相等的距离，与 `umbraLength()` 的返回值应相差 **< 1 km**。对不上就说明 `dist` 用错了。

**这两个数字的意义，是整个作品的教学核心，务必理解：**

> 月球本影锥长 373,408 km。观察者站在**地表**，比地心近一个地球半径，
> 所以实际临界地心距 = 373,408 + 6,371 = **379,779 km**。
> 而地月平均距离是 **384,400 km —— 比临界值还远**。
>
> **也就是说，平均情况下月亮的影子尖端根本够不到地球。**
> 只有月亮跑得比约 379,800 km 更近时才能看到日全食，否则只能看到日环食。
> 这就是日环食的全部原理，也是本作第 3 章的高潮。
>
> 而地球本影锥长 1,382,030 km，是地月距离的 3.6 倍，所以月亮总能整个钻进去 —— 月全食才可能发生。

**地球影子在月球所在处的尺寸（月食章节必需）：**
```js
const rUmbraAtMoon    = R_EARTH * (1 - D_MOON_MEAN / 1382030);          // ≈ 4599 km
const rPenumbraAtMoon = R_EARTH + D_MOON_MEAN * Math.tan(penumbraHalfAngle(R_EARTH, R_SUN, D_SUN_MEAN)); // ≈ 8176 km
```
换算成孩子听得懂的话：地球本影 ≈ **2.65 个月亮宽**（"两个半月亮还多"），半影 ≈ **4.71 个月亮宽**。

**半角期望值（自测用）**：月球本影半角 **0.2666°**、月球半影半角 **0.2679°**、地球本影半角 **0.2641°**、地球半影半角 **0.2690°**。

## 2.4 两圆相交面积 —— 全作的数学核心，只实现一次，四处复用

用于：① 地表食分着色 ② 天空视图里太阳被咬的形状 ③ 月食时月面明暗 ④ 天空亮度。

```glsl
#define PI 3.141592653589793

float circleIntersectArea(float r1, float r2, float d) {
    if (d >= r1 + r2) return 0.0;                    // 相离
    float rmin = min(r1, r2);
    if (d <= abs(r1 - r2)) return PI * rmin * rmin;  // 内含
    d = max(d, 1e-6);
    float d1 = (d*d + r1*r1 - r2*r2) / (2.0 * d);
    float d2 = d - d1;
    float a1 = r1*r1 * acos(clamp(d1/r1, -1.0, 1.0)) - d1 * sqrt(max(r1*r1 - d1*d1, 0.0));
    float a2 = r2*r2 * acos(clamp(d2/r2, -1.0, 1.0)) - d2 * sqrt(max(r2*r2 - d2*d2, 0.0));
    return a1 + a2;
}

// 太阳被遮住的面积比例 [0,1]
float obscuration(float rSun, float rMoon, float d) {
    return circleIntersectArea(rSun, rMoon, d) / (PI * rSun * rSun);
}
```

同时写一份等价的 JS 版本用于逻辑判断。

## 2.5 食的分类判据

设 `rS` = 太阳角半径，`rM` = 月亮角半径，`d` = 两者角距离：

```js
const magnitude = (rS + rM - d) / (2 * rS);   // 食分，按直径定义（天文标准）

if (magnitude <= 0)        return '无食';
if (magnitude < 1)         return '日偏食';
if (rM >= rS)              return '日全食';
else                       return '日环食';
```

月食（`rMoon` 月球角半径，`rU` 本影角半径，`rP` 半影角半径，`d` 月心到影心角距）：

```js
if (d >= rP + rMoon)       return '无食';
if (d >= rU + rMoon)       return '半影月食';   // 几乎看不出来
if (d >  rU - rMoon)       return '月偏食';
else                       return '月全食';
```

## 2.6 天空亮度曲线 —— 最容易做错的地方

**真实体验是：日偏食遮到 80% 时天空几乎没变暗**，因为人眼自适应能力极强、且散射光来自整个天空。只有最后几个百分点，天才会突然黑下来。

**很多演示把亮度做成线性，这是错的，而且毁掉了日全食的戏剧性。必须用下面的非线性映射：**

```glsl
float lum = 1.0 - obsc;                       // 剩余光通量
float adapted = pow(lum, 0.18);               // 人眼亮度适应
float cliff = smoothstep(0.0, 0.006, lum);    // 最后 0.6% 的断崖
float skyBrightness = adapted * (0.03 + 0.97 * cliff);
```

**自测（这张表是硬性基准，实现后必须核对）：**

| 遮挡 | lum | skyBrightness | 观感 |
|---|---|---|---|
| 0% | 1.0 | **1.000** | 大白天 |
| 50% | 0.5 | **0.883** | 几乎没感觉 ✅ |
| 90% | 0.1 | **0.661** | 微微发暗 |
| 99% | 0.01 | **0.437** | 有点像阴天，但天还亮着 |
| 99.7% | 0.003 | **0.181** | 明显暗了，像黄昏 |
| 99.9% | 0.001 | **0.029** | 天黑了 |
| 100% | 0 | **0.000** | 星星与日冕可见 |

注意 99% → 99.9% 之间的骤降（0.437 → 0.029）—— **这个断崖就是日全食的戏剧性所在**，也是真实观测中"最后几秒天突然黑下来"的来源。孩子拖到 99% 时应该还觉得"没什么嘛"，然后最后一下子全黑。

星星淡入用 `smoothstep(0.35, 0.05, skyBrightness)`：99% 时星星仍不可见，99.7% 时半可见，99.9% 时全可见。

### 推论：日环食时天空几乎不会变暗（必须实现）

把这条曲线套到环食上，会得到一个大多数人不知道的事实：

| 情形 | 最大遮挡 | 剩余金环 | skyBrightness |
|---|---|---|---|
| 远地点日环食 | 84.3% | 15.7% | **0.716** |
| 平均距离日环食 | 94.4% | 5.6% | **0.596** |
| **日全食** | 100% | 0 | **0.000** |

**环食再"深"，天空亮度也还有 0.6–0.7，几乎不变暗**，因为剩下那圈金环仍在发出强光。

这是环食与全食**最大的体验差别**，比"形状不一样"重要得多。第 3 章必须让孩子感受到：拖过临界点的一瞬间，不只是形状从"全黑"变成"金环"，**整个天空的亮度也从黑夜跳回白天**。这个反差会让"影子够不够得到"真正刻进脑子里。

## 2.7 血月颜色

月球进入本影后，唯一光源是穿过地球大气边缘折射进来的阳光。蓝光被瑞利散射掉，只剩红光。

```glsl
// t = 进入本影的深度：0 = 影缘，1 = 影心
vec3 EDGE   = vec3(0.90, 0.45, 0.18);
vec3 MID    = vec3(0.76, 0.27, 0.06);
vec3 CENTER = vec3(0.42, 0.11, 0.03);
vec3 blood = t < 0.5 ? mix(EDGE, MID, t*2.0) : mix(MID, CENTER, (t-0.5)*2.0);
// 整体亮度提到 0.12–0.35 区间以便可见
```

**本影内的月亮必须是清晰可见的红色，不是黑色。**
**半影区必须做得几乎看不出来**（亮度衰减 < 15%）—— 这正是"半影月食为什么没人注意"的教学点。

## 2.8 尺度系统（架构上最关键的决策）

真实比例画不进屏幕（太阳画成 100px，地球就是 0.9px，且在一万像素之外）。但**随便夸张会让影锥角度全错**，因为影锥形状恰恰由真实比例决定。

**解法：分离三套坐标。**

| 坐标系 | 用途 | 是否受尺度旋钮影响 |
|---|---|---|
| **物理坐标**（km，真实值） | 一切科学计算：角度、遮挡、影锥、分类 | ❌ 永远真实 |
| **渲染坐标**（场景单位） | Three.js 里天体的实际摆放 | ✅ 受旋钮影响 |
| **角度坐标**（弧度） | 「小朋友的眼睛」天空视图 | ❌ 永远真实 |

```js
// 尺度旋钮 k ∈ [0,1]，0 = 真实比例，1 = 最夸张
function renderRadius(realR, k)  { return realR * Math.pow(EXAG_MAX, k) * SCENE_UNIT; }
function renderDistance(realD, k){ return realD * Math.pow(COMP_MAX, k) * SCENE_UNIT; }
```

⚠️ **影锥在渲染坐标下必须重新代入 2.3 节的公式计算，绝不能对真实锥体做缩放。**
⚠️ 夸张尺度会让"够不够得到"的临界点偏移。**推荐做法：让"哪种食"的判定始终由物理坐标决定，渲染坐标只负责好看。** 即 `classifyEclipse()` 只吃真实 km，UI 显示的类型、食分、天空亮度全部来自它；影锥的 3D 形状单独按渲染坐标算，并**强制让锥尖位置与物理判定一致**（物理判定为全食，就保证锥尖插进渲染的地球里）。这样永远不会出现"画面显示锥尖够到了，但文字说是环食"的自相矛盾。

若选择校准渲染几何的方案，则写一个显式的 `calibrateShadowCone()` 函数调整距离压缩率，**使临界地心距落在真实的 379,779 km 附近**，并加注释说明。不要靠试出来的魔数。

界面上必须**永远显示**当前放大倍数，例如："🔍 图中天体已放大 43 倍，距离已缩短 55 倍"。

尺度旋钮本身就是教学内容：孩子把它拖到"真实"那端时会看到地球月亮缩成小点、距离拉开 —— 这本身就是一课。

---

# 三、结构：六章 + 沙盒

每章节奏固定：**【看】自动短演示（可跳过）→【玩】可拖控件 + 旁白 →【问】一个小挑战 →【收】徽章点亮**。

## 序章 · 三个球
认识太阳、地球、月亮。点击天体弹出名片卡，镜头推近。介绍尺度旋钮。无挑战。

## 第1章 · 一样大的巧合
**教：视直径。** 太阳比月亮大 400 倍，也远 400 倍，所以看起来一样大 —— 这是日全食能存在的唯一原因。

分屏：左边"真实大小对比"（2D 绘制即可），右边天空视图（两者一样大）。
控件：「月亮有多大」滑杆（0.3×–2.0× 真实半径）、「月亮有多远」滑杆（0.5×–2.0×）。数值读出用"是太阳的 1.02 倍大"这种说法，不用角分。
挑战：把月亮调到刚好盖住太阳（±3% 容差）。

## 第2章 · 影子的秘密
**教：本影与半影。** 故意脱离太空场景，用**台灯 + 小球 + 墙**做日常锚点。

控件：拖动小球前后、「灯有多大」滑杆、切换到墙上视角。
两个关键状态必须做出来：
- **把灯缩成一个点 → 半影完全消失**（这是本影/半影成因的核心证据）
- **把小球拉远 → 墙上的本影消失，只剩灰环**（这是日环食的几何原理，为第 3 章埋雷）

## 第3章 · 日食三兄弟 ★核心
三区布局：主 3D 视图（含影锥）+ 右下「小朋友的眼睛」PIP + 左下可点选的迷你地球。

控件：时间轴（可刮擦 + 播放）、**「月亮有多远」滑杆（356,500–406,700 km，本章灵魂控件）**、观察点选择、三个预设按钮（全食/偏食/环食）。

**必须做对的四件事：**
1. **影锥够不够得到地球要一眼看出来。** 全食时锥尖插进地球；环食时锥尖悬在上方，画一条虚线延长到地球并标注"差 XXXX 公里"。
2. **观察点在本影内 / 半影内 / 影外，PIP 视图必须完全不同。**
3. **天空亮度必须非线性**（见 2.6）。
4. **全食瞬间**：日冕淡入 + 钻石环 + 星星浮现 + 地平线一圈橙色（那是远处还在半影里的地方）+ 音效 + **至少 2 秒的停顿**。这是全作情绪高潮。

挑战：让小朋友站的地方看到日全食（需同时调好距离和站位）。

## 第4章 · 月食与血月
视角切到地球背后，看地球拖出的巨大影锥。

控件：时间轴、「月亮走高一点/低一点」滑杆（→ 全食/偏食/半影食）、**「站到月亮上去」按钮**。

**「站到月亮上」视图是本章王牌，务必实现**：从月球回看地球，地球是黑盘，边缘围着一圈明亮的橙红光环。旁白："那是地球上所有地方的日出和日落，同时发生。就是这一圈光，染红了你脚下的月亮。"

必须给出的对比：日全食只有几分钟且只有一条窄线能看到；月全食能看一个多小时且夜半球都能看到 —— 因为地球影子大太多了。

挑战：造出一次半影月食，然后指出"几乎看不出来吧"。

## 第5章 · 为什么不是每个月？★难点
**教：白道倾角 5.145° 与交点。** 这是全作高潮，也是最容易被同类产品省略的一点 —— **不许省**。省掉它孩子会带着"日食应该每个月都有"的错误模型离开。

正侧视图：黄道面画成半透明浅色圆盘，白道面画成倾斜圆盘，交出一条交点连线。

控件：「一年」时间轴、自动快进播放、**「把路拉平」开关（倾角 5.145° ↔ 0°）**。
影子命中地球时全屏闪一下 + 音效 + 计数 +1。

「把路拉平」是本章的证明手段：孩子亲眼看到"如果不斜就月月都有"，反过来就懂了"因为斜所以稀有"。

挑战：猜一年能打中几次（2次 / 4次左右 / 12次），答案是 4 次左右（最少 2，最多 5）。

## 沙盒 · 自由观测台
全参数解锁 + 「随机来一次！」按钮（随机生成参数并说出是哪种食）。
参数：月球距离、轨道倾角、观察点、时间、尺度旋钮、太阳距离、各种显示开关。

---

# 四、视觉与交互规范

## 4.1 配色

```
深空背景  #070b1f → #1a1040（渐变，不用纯黑）
太阳      #ffb547 → #fff4d6（核心过曝到近白）
日冕      #fff8e7 低不透明度，靠 bloom 出效果
地球海洋  #1c4f8f    陆地 #3f7a4a → #8a7a4e    大气辉光 #5ba3e0
月球      高地 #c8c4bc    月海 #7a7770
本影      #2a1f5c α=0.55（深紫，不用纯黑 —— 纯黑在深空背景上看不见）
半影      #5b7ac4 α=0.16
血月      #c1440e → #6b1d08
主文字    #eef2ff    次文字 #9aa4c8    强调 #ffd166
面板      rgba(18,22,48,0.62) + backdrop-filter: blur(20px)
```

## 4.2 天体外观（全部程序化）

- **太阳**：球体 + 多层 FBM 湍流噪声随时间流动，中心白到边缘橙；外层加法混合球做色球；再一层 sprite 光晕；靠 Bloom 收尾。
- **地球**：3D simplex 噪声阈值化生成大陆，海洋加镜面高光；独立云层球（半径 ×1.01，缓慢自转）；大气用背面渲染的 Fresnel 辉光球（半径 ×1.05）；夜面混入城市灯光点。
- **月球**：fbm 噪声做月海暗斑 + 多频率凹坑法线扰动做环形山。
- **星空**：6000 个 Points，球面随机分布，大小亮度按幂律（少量亮星 + 大量暗星），轻微色温变化。

需要一份 3D simplex noise（Ashima/webgl-noise 经典实现，约 60 行 GLSL）+ fbm 包装，全场景共用，只写一遍。

**目标是干净、有辨识度、有一点手绘的温度。不要追求 NASA 照片级写实** —— 太写实反而不利于孩子抓住重点。

## 4.3 影锥画法（最重要的视觉元素）

- 本影锥用 `ConeGeometry`，半影锥用带不同上下半径的截头锥。
- 自定义着色器做 **Fresnel 边缘增强**，让它像"光的体积"而不是"一个塑料锥子"：
```glsl
float fres = pow(1.0 - abs(dot(normalize(vNormal), normalize(vViewDir))), 2.0);
float alpha = uBaseAlpha * (0.35 + 0.65 * fres);
alpha *= smoothstep(1.0, 0.75, vAxialT);   // 沿轴渐隐，避免锥底出现生硬的圆
```
- 所有半透明对象设 `depthWrite: false`、`side: DoubleSide`，用 `renderOrder` 显式排序：天体 → 影锥 → 大气 → 日冕 → 星空叠加。
- 本影锥尖端画一个标记点；环食时画虚线延长线 + 距离标注。
- 提供"线框模式"开关，只画锥的轮廓线。

## 4.4 「小朋友的眼睛」视图

**不要用真 3D 相机渲染这个视图。** 视场角 0.5° 时近远裁剪面精度会爆炸、太阳月亮会抖、日冕做不出来。

**用一个 2D 全屏片元着色器直接画**：`vUv` 映射到角度坐标，视场默认 4°（可变焦 1°–15°）。

绘制层次（后 → 前）：
1. 天空底色（亮度按 2.6 的曲线）
2. 星星（按 skyBrightness 淡入）
3. 地平线一圈橙色（仅全食时）
4. 日冕（遮挡 ≥ 99% 时淡入；径向流动噪声，各向异性拉伸）
5. 太阳圆面（过曝白心 + 金边）
6. 月亮圆面（极暗带一点地球反照的暗蓝，边缘略带锯齿表现月面山脉）
7. 贝利珠 / 钻石环（食既/生光前后 ±1.5% 遮挡窗口内，沿月亮边缘 1–3 个亮点）

同一套着色器换参数后也用于月食视图。

## 4.5 地表食分着色

**不要用 shadow map**（会有锯齿、闪烁、本影半影分不清、尺度一改就乱）。

在地球片元着色器里，对每个片元：算出它看到的太阳角半径、月亮角半径、两者角距离 → 调 `obscuration()` → 用结果调制光照。这样影子是**真实计算出来的**，本影到半影的过渡天然连续。

## 4.6 UI

```
┌────────────────────────────────────────────────────────┐
│  ╭──────────────────────╮          ● ● ● ● ● ● ○      │ 章节导航
│  │ 旁白气泡（打字机）    │                             │
│  ╰──────────────────────╯                             │
│                  3D 主视图                             │
│                                      ╭──────────────╮  │
│  ╭────────────╮                      │ 👁 小朋友的眼睛│  │
│  │ 🌍 迷你地球│                      ╰──────────────╯  │
│  ╰────────────╯                                       │
│  ╭──────────────────╮   ╭──────────────────────────╮  │
│  │ 月亮有多远 ●───  │   │ ◀◀ ▶ ▶▶  ├──●─────────┤ │  │
│  │ 尺度      ──●──  │   ╰──────────────────────────╯  │
│  ╰──────────────────╯                      时间轴      │
└────────────────────────────────────────────────────────┘
```

- 滑杆高度 ≥ 36px，滑块 ≥ 28px，**必须有跟着滑块走的实时数值气泡**，跨越关键阈值时给音效 + 一次高亮闪动。
- 按钮圆角 ≥ 16px，点击区 ≥ 44×44px，按下 `scale(0.96)`。
- 时间轴支持点击跳转 + 拖动刮擦 + 播放/暂停 + 倍速。
- 旁白打字机逐字出现（约 45 字/秒），点击可跳过直接全显。
- 与当前章节无关的面板**淡出移除**，不要留着占地方。
- 相机：OrbitControls，禁用 pan，限制极角，阻尼 0.08。章节切换时相机做 1.2 秒 `easeInOutCubic` 过渡，不许瞬移。**禁止自动旋转相机**（会晕，且干扰观察）。
- 字体 `-apple-system, "PingFang SC", "Hiragino Sans GB", sans-serif`；旁白 20–22px / 行高 1.75；数字用 `tabular-nums`。
- 过渡统一 `cubic-bezier(0.4,0,0.2,1)`，200–400ms。
- **禁止闪烁、快速抖动、高频颜色循环**（对儿童不友好）。

## 4.7 后期处理

`UnrealBloomPass`（强度 0.7 / 半径 0.5 / 阈值 0.85）+ 轻微 vignette（0.25）。
`toneMapping = ACESFilmicToneMapping`，`exposure ≈ 1.1`，`devicePixelRatio` 上限 2。
**不要**加色差、胶片颗粒、镜头光晕 —— 会干扰"看清楚"这个首要目标。

## 4.8 音效（Web Audio API 程序化合成，零文件）

| 事件 | 音效 |
|---|---|
| 按钮点击 | 880Hz 正弦 pop，60ms |
| 跨越关键阈值 | 轻微上滑音 |
| 挑战成功 | C–E–G 上行琶音 |
| 徽章点亮 | 正弦 + 三倍频铃声，1.2s 衰减 |
| 全食瞬间 | 低频渐强 + 骤停 |
| 影子命中地球 | 清脆"叮" |

必须有总静音开关，状态存 `localStorage`。**首次用户交互前不播放任何声音**（浏览器 autoplay 策略）。

---

# 五、代码组织

单文件内按此顺序，每段用醒目注释分隔条隔开：

```
<style> …全部 CSS，约 400 行… </style>
<body>
  …DOM 骨架（面板 / 旁白 / 导航），约 150 行…
  <script type="importmap">…</script>
  <script type="module">
    ═══ 1. 常量 ═══           天文常数、颜色、尺度参数
    ═══ 2. 数学工具 ═══        angularDiameter / circleIntersectArea /
                              umbraLength / classifyEclipse / 缓动
    ═══ 3. 着色器源码 ═══      noise(共用) / sun / earth / moon /
                              shadowCone / skyView
    ═══ 4. 场景搭建 ═══        renderer / stars / sun / earth / moon /
                              cones / lampScene
    ═══ 5. 状态模型 ═══        State（单一真相源）+ derive() + 订阅
    ═══ 6. 「小朋友的眼睛」═══    独立 render target + 全屏 quad
    ═══ 7. UI 层 ═══           slider / button / Narrator / Timeline /
                              BadgeSystem / MiniGlobe
    ═══ 8. 音效 ═══            AudioKit: tone / chord / sweep
    ═══ 9. 章节定义 ═══        CHAPTERS = [{ id, title, setup, teardown,
                                controls, narration, challenge, camera }]
    ═══ 10. 主循环与启动 ═══   animate / onResize / boot
  </script>
```

importmap：
```html
<script type="importmap">
{
  "imports": {
    "three": "https://unpkg.com/three@0.169.0/build/three.module.js",
    "three/addons/": "https://unpkg.com/three@0.169.0/examples/jsm/"
  }
}
</script>
```

**状态模型原则**：所有派生量（角半径、食分、遮挡率、影锥长度、食的类型、天空亮度…）**每帧从 State 重算**，不做增量更新。这个规模下性能完全够，能避免大量状态同步 bug。

**资源管理**：几何体只创建一次，用 `scale`/`position`/uniform 更新，**不许每帧重建几何体**。写一个递归的 `disposeObject3D(obj)`，章节 `teardown()` 时调用。

预估总量 **2200–3000 行**（含注释与文案）。

---

# 六、文案（语气基准 + 关键段落）

**语气基准**：像家长在旁边指着屏幕说话。短句。多用"你"。允许感叹。避免"我们"这种教科书腔。专业名词第一次出现时加粗，并立刻用大白话解释。

**写完每段文案后大声念一遍 —— 如果听起来不像在跟一个 8 岁孩子说话，重写。**
❌ 反例："由于月球轨道平面与黄道面存在约 5.145° 的夹角……"

下面是几个关键段落，**可直接使用**。其余章节按同样语气自行补全（每章 10–25 句）。

**第1章核心**：
```
太阳比月亮大 400 倍。
可是你看看天上 —— 太阳和月亮，看起来居然差不多大！
为什么？因为太阳虽然大了 400 倍，但它也远了 400 倍。
大 400 倍，远 400 倍，一抵消，就一样大了。
整个太阳系，只有站在地球上，才能看到这么刚刚好的巧合。
```

**第2章核心**：
```
你看墙上的影子 —— 它不是一整块黑的。
中间那块，最黑最黑，一点光都照不进来。这叫本影。
边上那圈，灰灰的，还有一点点光。这叫半影。
为什么会有两层呢？因为灯不是一个小点，它是有大小的。
【把灯变小，变成一个小点】看！半影不见了。
【把小球往后拉远】墙上最黑的那块越来越小……再远一点……不见了！
记住这个 —— 待会儿在天上，你会看到一模一样的事情。
```

**第3章 · 站在本影里**：
```
天黑了！
太阳完全不见了，只剩下周围一圈白白的光 —— 那叫日冕，
是太阳的"头发"，平时被太阳自己的光盖住，只有这时候才看得见。
星星出来了。远处的天边还是亮的，像一圈日落。
这就是日全食。一辈子能看到一次，都算幸运。
```

**第3章 · 环食**：
```
月亮不是每次都一样近的。它绕地球走的路是一个椭圆。
【慢慢把月亮拉远】看月亮影子的尖尖 —— 它变短了。再远一点……
它够不着地球了！就像刚才那个小球和墙一样。
这时候站在地上看，太阳的中间被盖住了，可是边上还剩下一整圈金光。
这就是日环食 —— 天上的一枚金戒指。
```

**第3章 · 临界点**：
```
就是这里！
月亮离地球 37 万 9 千公里，就是全食和环食的分界线。
比这个近一点 → 日全食。远一点 → 日环食。

有意思的是 ——
月亮平均离我们 38 万 4 千公里，比这条线还要远一点点。
所以其实，日环食比日全食还要多见一些。
```

**第3章 · 环食时的天空**（配合 2.6 节推论）：
```
咦，明明太阳被盖住了这么多，天怎么还是亮的？
因为剩下的那一圈金光，还是很刺眼很刺眼。
日环食的时候，天不会黑，星星也不会出来。
只有日全食，才会让白天突然变成黑夜。
```

**第4章 · 血月**：
```
你还记得傍晚的太阳吗？为什么是红色的？
因为阳光穿过厚厚的空气时，蓝色的光被弹开了，只有红色的光能穿过去。
地球周围也包着一层空气。
阳光擦着地球边上飞过的时候，被这层空气弯了一下，偷偷送了一点点进影子里。
而这点光，是红色的。
所以月亮就被染红了。人们叫它 —— 血月。
```

**第4章 · 站到月亮上**：
```
你现在站在月亮上，抬头看地球。
地球是黑的，它把太阳完全挡住了。
可是地球的边上，围着一圈红红的光环。
你知道那是什么吗？
那是地球上所有地方的日出和日落，同时发生。
就是这一圈光，染红了你脚下的月亮。
```

**第5章核心**：
```
现在有一个问题。月亮每个月都会转到地球和太阳中间一次。
那……为什么不是每个月都有日食呢？
【换个角度看】
地球绕太阳走的路，是这个平平的圆盘。
月亮绕地球走的路，是这个斜斜的圆盘。它们差了大概 5 度。
5 度听起来很小，可是在天上，这就够了。
【按播放，看一整年】
大部分时候，月亮从太阳的上面飘过去了……或者从下面飘过去了……
影子扔到了太空里，没有打中地球。
只有在两个圆盘交叉的那两个地方 —— 我们叫它交点 —— 月亮才会挡个正着。
【把月亮的路拉平，变成 0 度试试】哇，每个月都有日食了！
现在拉回 5 度。看，又变回来了。
所以，日食和月食是很珍贵的。就因为月亮走的那条路，斜了那么一点点。
```

**结业总结卡**：
```
小朋友，你现在知道了六件事：

☀️ 太阳比月亮大 400 倍，也远 400 倍 —— 所以看起来一样大
🌓 影子有两层：中间的本影，和外面的半影
🌑 月亮的影子扫过地球，扫到的地方是日全食，旁边是日偏食
💍 月亮跑远了，影子够不着地球，就变成日环食
🔴 月亮走进地球的影子会变红，因为地球的空气把红光弯了进来
📐 月亮的路斜了 5 度，所以日食月食才这么少见

下一次真的有日食的时候，你就知道天上正在发生什么了。
```

---

# 七、交付前自检清单

**在交付前，请逐条核对并在回复中报告结果。发现不通过的项，先修好再交付。**

## 科学正确性
- [ ] 写一个 `runSelfTest()` 挂到 `window` 上，控制台可运行
- [ ] 2.2 节六个角直径值全部对上（误差 < 0.5%）
- [ ] 月球本影锥长 = **373,408 km** ± 200（`dist` 必须用日月距离）
- [ ] 地球本影锥长 = **1,382,030 km** ± 2,000
- [ ] **自洽性**：二分法解出的"角径相等距离"与 `umbraLength()` 相差 < 1 km
- [ ] 地球本影在月距处半径 = **4,599 km** ± 30
- [ ] 月球距离 356,500 km 正对 → 「日全食」（食分 1.024）
- [ ] 月球距离 **384,400 km（平均）** 正对 → 「**日环食**」（食分 0.986）
- [ ] 月球距离 406,700 km 正对 → 「日环食」（食分 0.959）
- [ ] 临界切换（地表观察者）发生在 **379,779 km** ± 1,000
- [ ] 2.6 节天空亮度表七个值全部对上（尤其 50%→0.883、99%→0.437、99.9%→0.029）
- [ ] 遮挡 99% 时星星**不**可见，99.9% 时可见
- [ ] **远地点环食最深时 skyBrightness ≈ 0.72，天空明显仍是亮的**
- [ ] 半影月食下月面亮度衰减 < 15%
- [ ] 倾角 0° → 每个朔望月都有食；倾角 5.145° → 一年 2–5 次

## 教学有效性
- [ ] 每章至少一个可拖控件，拖动时画面实时响应
- [ ] 「小朋友的眼睛」任何时刻都与主视图物理状态一致
- [ ] 环食时锥尖到地球的间隙有虚线 + 距离标注
- [ ] 观察点从本影移到半影时 PIP 有显著且正确的变化
- [ ] 尺度旋钮拖到真实端时界面显示当前放大倍数
- [ ] 第5章「把路拉平」开关能立刻产生对比效果
- [ ] 第2章把灯缩成点时半影完全消失

## 工程质量
- [ ] 单个 `.html`，双击可在 Chrome 和 Safari 打开
- [ ] 无外部资源（除 three.js CDN）
- [ ] 控制台无 error、无 warning
- [ ] 窗口从 1920×1080 缩到 1024×640 布局不崩
- [ ] 快速连续切章无内存泄漏（`teardown()` 有 dispose）
- [ ] 任何滑杆拖到极值都不产生 NaN、不黑屏
- [ ] 首次交互前不播放声音
- [ ] 天文常数集中在顶部、命名清晰、无魔数
- [ ] 关键公式处有注释标明含义

## 体验
- [ ] 60fps @ 2× DPR
- [ ] 打开到可交互 < 2 秒
- [ ] 章节切换有平滑相机过渡
- [ ] 全食瞬间有 ≥ 2 秒的戏剧性停顿
- [ ] 无闪烁、无高频抖动
- [ ] 全部文案适合 7–9 岁

---

# 八、十个必须避开的陷阱

0. **影锥公式的 `dist` 用错** —— 算月球影锥时代入了**日地**距离而不是**日月**距离。这会让锥长多算约 960 km，且与"角径相等距离"对不上，从而让全食/环食的临界点偏移。**用 2.3 节的自洽性检查（二分法解角径相等距离，应与 `umbraLength()` 相差 < 1 km）来抓这个错误。**

1. **尺度陷阱** —— 把地球放大 50 倍却不重算影锥，结果锥子包住整个地球，看起来日食随时在发生。**必须在渲染坐标下重新代入公式算锥长和半角，并显式校准临界点。**

2. **用 shadow map 画影子** —— 会有锯齿、闪烁、本影半影分不清、尺度一改全乱。**用 2.4 的解析公式在片元着色器里算。**

3. **线性天空亮度** —— 偏食刚开始天就明显变暗，全食时已无惊喜。**必须用 2.6 的非线性曲线。** 这不只是"更真实"，它是整个作品情绪节奏的关键。

4. **月食做成全黑** —— 血月出不来。**本影内必须是清晰可见的红色**，半影区必须几乎看不出来。

5. **透明物体排序错乱** —— 影锥、大气辉光、日冕互相穿插闪烁。**统一 `depthWrite:false` + 显式 `renderOrder`。**

6. **「小朋友的眼睛」用真 3D 相机** —— 0.5° 视场下精度爆炸、天体抖动、日冕做不出来。**用 2D 全屏着色器。**

7. **每帧重建几何体** —— 拖滑杆越来越卡，内存持续上涨。**几何体只建一次，靠 uniform 和 transform 更新。**

8. **文案写成教科书** —— 出现"由于月球轨道平面与黄道面存在约 5.145° 的夹角"这种句子。**照第六节的语气基准写。**

9. **滑杆没有实时反馈** —— 孩子拖了但不知道改了什么。**每个滑杆必须有数值气泡 + 至少一个立刻变化的画面元素 + 跨阈值时的音效和高亮。**

---

# 九、交付格式

输出**一个完整的 HTML 文件**，用一个代码块包裹。

代码块之后，附一份简短报告：
1. 第七节自检清单的逐项结果（通过 / 未通过 + 原因）
2. `runSelfTest()` 的实际输出数值
3. 你做出的任何偏离本文档的设计决策，以及理由
4. 已知限制或未实现的部分（如果有）

**不要**在代码里留任何占位符。**不要**省略文案。**不要**因为"篇幅原因"缩减实现。如果单次输出长度不够，明确说出你写到了哪一节、下一次从哪里继续，然后分次输出完整代码。

═══ 提示词结束 ═══

---

# 分批指令（可选）

如果开发模型一次写不完 3000 行，用下面的分批方式。**每一批都必须是可独立运行的完整状态**，不是半成品。

## 第 1 批 · 骨架 + 数学
> 只做：HTML/CSS 骨架、Three.js 场景、星空、三个天体（先用简单 MeshStandardMaterial）、OrbitControls、主循环、章节导航壳、以及第二节全部公式的 JS 实现 + `runSelfTest()`。
>
> 验收：能打开、能转视角、看到三个球；控制台跑 `runSelfTest()` 全部通过。

## 第 2 批 · 第3章（日食核心）
> 在第 1 批基础上加：影锥几何 + 地表食分着色器 + 「小朋友的眼睛」着色器 + 月亮距离滑杆 + 观察点选择 + 时间轴。
>
> 验收：能拖出全食 / 偏食 / 环食三种状态，PIP 里能看到三者的显著差异；环食时锥尖间隙有标注。

## 第 3 批 · 第4章（月食）
> 加：地球影锥 + 月面着色（含血月）+「站到月亮上」视角 + 高度滑杆。
>
> 验收：能拖出月全食 / 月偏食 / 半影月食；半影月食几乎看不出来；血月是红色的。

## 第 4 批 · 天体外观
> 把三个天体的简单材质换成程序化着色器：太阳湍流、地球大陆/云层/大气辉光/城市灯光、月球环形山。加 Bloom 和 vignette。
>
> 验收：好看，且帧率仍然 60fps。

## 第 5 批 · 序章 + 第1、2章
> 加：天体名片卡、视直径分屏对比、台灯场景。
>
> 验收：前半程完整可玩；灯缩成点时半影消失；小球拉远时本影消失。

## 第 6 批 · 第5章（倾角）
> 加：黄道/白道双圆盘侧视图、一年时间轴、「把路拉平」开关、命中计数。
>
> 验收：能看到影子一次次擦肩而过与偶尔命中；拉平后变成月月都有。

## 第 7 批 · 打磨
> 加：旁白打字机、全部音效、徽章系统、各章挑战、沙盒模式、结业总结卡、localStorage 存档、性能模式开关。
>
> 验收：第七节自检清单全部通过。
